# backend/.env — local overrides for backend-only workflows (running the API or
# the Prisma CLI without docker compose). `scripts/setup.sh` derives this file
# from the root `.env` in local mode; the root `.env` is the source of truth.
# Never commit real secrets.

# Point at your database. Local/docker install: host 127.0.0.1:55432.
DATABASE_URL="postgresql://USER:PASSWORD@127.0.0.1:55432/aibom?schema=public"

# Required. Generate a strong value: openssl rand -hex 32
JWT_SECRET=""

PORT=4000

# Serve the built SPA (frontend/dist) from this process. Defaults to on when
# NODE_ENV=production, off otherwise. When on, the app is reachable at
# http://localhost:$PORT with no separate frontend server.
# SERVE_STATIC=true
# Override the SPA build location (default: ../frontend/dist relative to the build).
# FRONTEND_DIST=/app/frontend/dist

# Only needed when the SPA is served from a different origin than the API.
CORS_ORIGIN="http://localhost:4000"

# Set when running behind a reverse proxy: "1" for one hop, or a trusted subnet.
# TRUST_PROXY=1

# Initial admin account (seed time only — read by prisma:seed:reference).
# scripts/setup.sh prompts for these; set them here to seed non-interactively.
# ADMIN_EMAIL defaults to admin@example.com; a blank ADMIN_PASSWORD generates a
# random one and prints it. Setting ADMIN_PASSWORD on a later seed run resets it.
# ADMIN_EMAIL=admin@example.com
# ADMIN_NAME=Admin User
# ADMIN_PASSWORD=
