# Revisions to skip in `git blame`. Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub applies it automatically.

# style: adopt Prettier — repo-wide reformat, no behaviour change
# (hash filled in by the commit that does the reformat)
